name: React Build File

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build React App
    runs-on: macos-latest
    steps:
      - name: install dependencies
        run: npm i
      - name: build command
        run: npm run build
